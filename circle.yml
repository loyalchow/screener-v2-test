machine:
    node:
        version: 5.5.0

dependencies:
    override:
        - npm install

test:
    override:
        - >
	  if [ -n "${RUN_SCREENER}" ]; then
            npm run test-screener;
      	  fi	  
